%h1 Ajout d'élément

= form_for(@equipment) do |f|
	- if @equipment.errors.any?
		%ul
		- @equipment.errors.each do |a, message|
			%li.warning= message
	
	= f.label 'Nom :'
	= f.text_field :name 
	= f.label 'Item :'
	= f.text_field :item
	= f.label 'Emplacement :'
	= f.text_field :location
	= f.label 'Matière :'
	= f.text_field :material
	= f.label 'Prix :'
	= f.text_field :price
	= f.label 'Niveau :'
	= f.text_field :level
	= f.label 'Bonus :'
	= f.text_field :bonus
	= f.submit 'Ajouter', :class => 'btn'

= button_to "Retour", equipments_path, :method => :get, :class => 'btn'
